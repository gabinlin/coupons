spring:
  application:
    name: provider
  freemarker:
    prefer-file-system-access: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
management:
  endpoints:
    web:
      exposure:
        include: "*"
