spring:
  application:
    name: provider
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        serviceName: provider
  test:
    name: test10
server:
  port: 8094
management:
  endpoints:
    web:
      exposure:
        include: "*"
  server:
    port: 8094
